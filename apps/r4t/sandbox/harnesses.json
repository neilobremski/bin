{
  "_notes": "Sandbox harness config (live mode). Tier invoke is live-agent.py; the underlying LLM argv comes from `r4t sandbox --preset` (R4T_SANDBOX_INVOKE). `r4t sandbox --fake` swaps every invoke for fake-agent.py and drops the cadence gate.",
  "throttle": {
    "max_concurrent": 1,
    "min_seconds_between_turn_starts": 5
  },
  "suppression_window_seconds": 30,
  "leader": {
    "invoke": ["opencode", "run", "--auto", "--dir", ".", "{prompt}"],
    "timeout_seconds": 600,
    "max_turns_per_task": 15,
    "hop_limit": 6
  },
  "member": {
    "invoke": ["opencode", "run", "--auto", "--dir", ".", "{prompt}"],
    "timeout_seconds": 600,
    "max_turns_per_task": 15
  }
}
